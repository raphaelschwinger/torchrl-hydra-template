# @package _global_
# DQN on CartPole-v1.
# Mirrors torchrl/sota-implementations/dqn_trainer/config/config.yaml so this
# experiment is the canonical sanity check that the wiring works end-to-end.

defaults:
  - override /algorithm: dqn
  - override /environment: cartpole
  - _self_
